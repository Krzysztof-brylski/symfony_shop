{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button
                    type="submit"
                    class="text-gray-100 btn btn-success">
                Submit Post
            </button>
        {{ form_end(form) }}
    </div>
{% endblock %}
